/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // Safelist: 確保動畫類別在生產環境不被 purge
  safelist: [
    // 淡入動畫類別
    'opacity-0', 'opacity-100',
    'translate-y-0', 'translate-y-4', 'translate-y-6', 'translate-y-8',
    'scale-95', 'scale-100',
    // 延遲動畫
    'delay-100', 'delay-150', 'delay-300', 'delay-500', 'delay-700', 'delay-1000', 'delay-1200',
    // 過渡時間
    'duration-700', 'duration-1000', 'duration-1200',
    // 動畫曲線
    'ease-out',
  ],
  theme: {
    extend: {
      fontSize: {
        /* ═══════════════════════════════════════════
           標題系統（H1, H2, H3）- 保持足夠的視覺衝擊力
           ═══════════════════════════════════════════ */
        'h1-mobile': '32px',    // ↑ 從 28px 提升到 32px
        'h1-desktop': '48px',   // ↑ 從 40px 提升到 48px
        
        'h2-mobile': '24px',    // ↑ 從 22px 提升到 24px
        'h2-desktop': '36px',   // ↑ 從 32px 提升到 36px
        
        'h3-mobile': '20px',    // ↑ 從 18px 提升到 20px
        'h3-desktop': '28px',   // ↑ 從 24px 提升到 28px

        /* ═══════════════════════════════════════════
           內文系統 - 保證可讀性（最小 16px）
           ═══════════════════════════════════════════ */
        'body-lg': '18px',      // 大段落文本
        'body-base': '16px',    // 標準內文（最小安全值）
        'body-sm': '15px',      // 輔助說明文本（不低於 15px）
        
        /* ═══════════════════════════════════════════
           元件系統 - 標籤、Badge、輔助文本（最小 14px）
           ═══════════════════════════════════════════ */
        'label-lg': '15px',     // 卡片標題、表單標籤
        'label-base': '14px',   // 標籤、Badge（最小安全值）
        'label-sm': '13px',     // 時間戳、極小提示（謹慎使用）
        
        /* ═══════════════════════════════════════════
           保留舊的 Tailwind 預設值供向下相容
           ═══════════════════════════════════════════ */
        'xs': '14px',           // ← 從 12px 提升到 14px（曾用於 Badge）
        'sm': '16px',           // ← 從 14px 提升到 16px（曾用於導航、內文）
        'base': '18px',         // ← 保持基準
        'lg': '20px',           // ← 增強視覺層級
        'xl': '24px',           // ← 強調重點
      },
      lineHeight: {
        '1.5': '1.5',           // 新增：標題專用（緊密）
        '1.6': '1.6',           // 新增：標籤專用
        '1.7': '1.7',           // 保留：內文預設
        '1.8': '1.8',           // 保留：寬鬆段落
      },
      letterSpacing: {
        '0': '0em',             // 新增：標題用（緊密）
        '0.02': '0.02em',       // 新增：內文正常
        '0.05': '0.05em',       // 保留：原設定
      },
    },
  },
  plugins: [],
}
